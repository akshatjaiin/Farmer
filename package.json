{
  "name": "farmstart",
  "version": "1.0.0",
  "description": "FarmStart - A comprehensive farm management application",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm run start:prod",
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm start\"",
    "dev:build-and-start": "cd frontend && npm run build && cd ../backend && npm start",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.7",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.10.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "private": true
}
